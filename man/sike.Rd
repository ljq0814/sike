% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esir.R
\name{sike}
\alias{sike}
\title{Sliced Inverse Kendall's Tau Estimation (SIKE)}
\usage{
sike(
  y,
  X,
  K,
  type = c("standard", "refinement"),
  eigenmed = NULL,
  Sig = diag(ncol(X)),
  slices = 10
)
}
\arguments{
\item{y}{A numeric vector of response values of length \code{n}.}

\item{X}{A numeric matrix of predictors with dimension \code{n x p}.}

\item{K}{A positive integer specifying the dimension of the central subspace.}

\item{type}{A character string specifying the estimation method. Either
\code{"standard"} (default) or \code{"refinement"}. Partial matching is
supported.}

\item{eigenmed}{Either \code{NULL} (default) or a numeric scalar. If
\code{NULL}, the median eigenvalue is computed separately within each
slice. If a numeric value is provided, it is used as a fixed median
eigenvalue across all slices.}

\item{Sig}{A \code{p x p} symmetric positive definite matrix used for
standardization. Defaults to the identity matrix.}

\item{slices}{A positive integer specifying the number of slices. Defaults
to \code{10}.}
}
\value{
A \code{p x K} matrix whose columns are the estimated basis vectors
  of the central subspace.
}
\description{
Performs sufficient dimension reduction by estimating the central subspace
using sliced inverse Kendall's tau estimation. Supports a standard mode and
a refinement mode that selects directions via distance correlation.
}
